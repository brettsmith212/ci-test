{
  "name": "ci-feedback-demo",
  "version": "1.0.0",
  "description": "Demo of CI-feedback loop for AI agents",
  "scripts": {
    "test": "mocha tests/*.test.js",
    "build": "echo 'Build complete'",
    "lint": "echo 'Lint complete'"
  },
  "devDependencies": {
    "mocha": "^10.0.0",
    "chai": "^4.3.0"
  }
}
